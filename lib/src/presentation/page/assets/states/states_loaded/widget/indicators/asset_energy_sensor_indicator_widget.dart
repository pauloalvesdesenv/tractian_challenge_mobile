import 'package:flutter/material.dart';

class AssetEnergySensorIndicatorWidget extends StatelessWidget {
  const AssetEnergySensorIndicatorWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.bolt,
      color: Color(0xFF52C41A),
      size: 16,
    );
  }
}
